@model CustomerSite.Models.Echo
@{
    ViewBag.Title = "Customer Page";
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <p><h1>@ViewBag.Title.</h1>
                </p>

<section id="Echo1">
<h2>Echoing your name.</h2>
@using (Html.BeginForm(new { ReturnUrl = ViewBag.ReturnUrl })) {

    <form>
    <fieldset>
        <legend>Your Name</legend>
        <ol>
            <li>
                @Html.LabelFor(m => m.UserNameEcho)
                @Html.TextBoxFor(m => m.UserNameEcho)
              
            </li>
            
        </ol>
        
    
        <input type="submit" value="Echo" id="btnSave" onclick="DoValidation"/>
    </fieldset>
   </form>
}
</section>
                <p>
                <h2>@ViewBag.Message</h2>
                </p>
            </hgroup>
        </div>
    </section>
}


